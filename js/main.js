// Declare the variables
let a = 3;
let b = 5;
let c;

// Create the output string using backticks and new lines for better readability
let num = `
a + b: ${a + b}
a - b: ${a - b}
a * b: ${a * b}
a / b: ${a / b}
a % b: ${a % b}
a += b: ${a += b}
a -= b: ${a -= b}
a *= b: ${a *= b}
a /= b: ${a /= b}
a %= b: ${a %= b}
a == b: ${a == b}
a != b: ${a != b}
a > b: ${a > b}
a < b: ${a < b}
!a && !c: ${!a && !c}
!a || !c: ${!a || !c}
`;

// Print the output in the browser's console
console.log(num);

// Declare the variables
let first_name = "Denis";
let last_name = "Muthui";
let email = "rukw0001@algonquinlive.com";

// Create the expression
let output = `My name is ${first_name} ${last_name}. You can contact me at ${email}.`;

// Print output in the browser's console
console.log(output);